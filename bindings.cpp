#include <stdio.h>
#include "CImg.h"
using namespace cimg_library;

/*
char* readImage(char* filename ){
    CImg<unsigned char> image(filename);  
    return image;
}

int createEmptyCanvas(int width, int height, int channels, int color){
    canvas(width,height,1,channels, color);
    T* pixel_array = canvas.data()
    printf(pixel_array);
    return 1;
}
*/
